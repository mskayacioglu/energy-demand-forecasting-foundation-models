# --- Python environments ---
.venv/
venv/
env/
ENV/

# --- Python caches / build ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Jupyter ---
.ipynb_checkpoints/

# --- OS ---
.DS_Store
Thumbs.db

# --- Local history of superseded artifacts (zips, old runs, project scaffolding) ---
archive/

# --- Final outputs: metric tables + run metadata are versioned; heavy binaries are not ---
outputs/forecasts/
outputs/models/
outputs/train_logs/

# --- Data: raw .tsf is versioned; the derived long parquet is regenerable from it ---
data/*.parquet

# --- Local scratch dirs the standalone notebooks create when run from the repo root ---
eda_outputs/
